Testing guidance:

- This starter doesn't include automated tests. Recommended to add Vitest or Jest + React Testing Library.
- Example command to run tests after adding Vitest:

  npm install -D vitest @testing-library/react
  npm run test
